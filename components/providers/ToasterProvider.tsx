"use client";

import { Toaster } from "react-hot-toast";

/**
 * Renders a ToastProvider component that wraps the Toaster component.
 * @returns JSX.Element
 */
export const ToastProvider = () => {
  return <Toaster />
};